# zodiac-gui

The GUI client for zodiac (roadmap Phase 3): a third client on the existing
session socket, sharing `zodiac::client_core` with the TUI. See
`../docs/roadmap.md` and `../docs/decisions/0004-gui-stack.md`.
